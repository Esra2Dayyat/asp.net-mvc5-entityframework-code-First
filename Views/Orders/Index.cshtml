@model IEnumerable<amazon.Models.Order>

@{
    ViewBag.Title = "Index";
}

<h2> All Customeres</h2>

<p>
  
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Customeres.Namee)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GetOrder.Select_Order)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.shipToAdress.City)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OrderDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.GetOrder.Price)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Customeres.Namee)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GetOrder.ImgPath)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.shipToAdress.City)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OrderDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Totalorder)
        </td>
  
    </tr>
}

</table>
